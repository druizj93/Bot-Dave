services:
  - type: web
    name: telegram-ai-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    # For webhook, etc. Adapt for Telegram polling or webhook.